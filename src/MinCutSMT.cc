#include "MinCutSMT.h"

namespace clou {
  
  void MinCutSMTPass::runOnBasicBlock(llvm::BasicBlock& B, const std::vector<ST<llvm::Instruction>>& sts) const {
    
  }
  
  void MinCutSMTPass::runOnFunction(llvm::Function& F, const std::vector<ST<llvm::BasicBlock>>& sts) const {

  }
  
}
